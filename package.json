{
  "name": "issue-projector",
  "version": "0.0.6",
  "private": true,
  "scripts": {
    "build": "ncc build src/entrypoint.ts",
    "watch": "tsc -w -p .",
    "exec": "env $(cat .env 2>/dev/null | xargs) node dist/entrypoint.js"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@octokit/rest": "^18.0.9"
  },
  "devDependencies": {
    "@types/node": "^14.10.0",
    "@vercel/ncc": "^0.25.1",
    "typescript": "^4.0.3"
  }
}
